/*
 * IIFE to keep code clean and safe
 */
(function(){

    /*
     * Declaration of main angular module for this appllication.
     *
     * It is named placements and has no dependencies (hence the 
     * empty array as the second argument)
     */
    angular
        .module("placements", []);

})();
